/**
 * These functions handle basic preprocessing for customized (factory)
 * blocks rendering
 */

// import _b5Blocks from '../blocks/blocks'

export function _findInput(blocks) {
  /**
   * Find all inputs from inner blocks in codeCanvas
   */
}
